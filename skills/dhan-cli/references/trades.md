# dhan trades

```bash
dhan trades list --json --no-color
dhan trades order <order-id> --json --no-color
dhan trades history --from <date> --to <date> --json --no-color
```

Confirm history date flags with `dhan trades history --help`.
