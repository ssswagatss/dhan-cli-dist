# dhan config

Non-secret preferences only.

```bash
dhan config get --json --no-color
dhan config get <key> --json --no-color
dhan config set <key> <value> --json --no-color
```

Examples: `baseUrl` (sandbox), `redirectPort`. Credentials → `auth configure`.
