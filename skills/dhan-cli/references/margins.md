# dhan margins

Read-only margin calculators.

```bash
dhan margins order --file order.json --json --no-color
dhan margins multi --file basket.json --json --no-color
```

Confirm subcommand names via `dhan margins --help`.
