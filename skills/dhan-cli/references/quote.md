# dhan quote

Live quotes by **numeric security ID**. Requires Dhan Data API subscription.

```bash
dhan quote ltp <security-id>... --json --no-color
dhan quote ohlc <security-id>... --json --no-color
dhan quote full <security-id>... --json --no-color
```

Resolve IDs via `instruments search`. Keep baskets modest.
