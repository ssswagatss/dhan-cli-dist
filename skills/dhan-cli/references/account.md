# dhan account

```bash
dhan account profile --json --no-color
dhan account funds --json --no-color
dhan account ledger --json --no-color
```

Read-only. Requires stored access token. Check exact ledger flags with `--help`.
